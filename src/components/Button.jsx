const Button = (props) => {

    const handleClick = () => {
        
    }

    return (
        <div className='button'>
            {props.someText}
        
        </div>
    )
    
}

export default Button